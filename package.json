{
  "name": "npm-script",
  "version": "0.2.0",
  "description": "",
  "main": "index.js",
  "config": {
    "port": 3000
  },
  "scripts": {
    "lint:js": "eslint *.js",
    "lint:css": "stylelint *.less",
    "lint:json": "jsonlint test.json",
    "lint:md": "markdownlint --config .markdownlint.json *.md",
    "mocha": "mocha tests/",
    "check-serial": "npm run lint:js && npm run lint:css && npm run lint:json && npm run lint:md && npm run mocha",
    "check-parallel": "npm run lint:js & npm run lint:css & npm run lint:json & npm run lint:md & npm run mocha",
    "check-all": "npm-run-all lint:js lint:css lint:json lint:md mocha",
    "check-all-simple": "npm-run-all lint:* mocha",
    "check-all-parallel": "mocha",
    "//": "test整个流程: 先检查代码，然后单元测试，最后进行测试覆盖率",
    "lint:js:fix": "npm run lint:js -- --fix",
    "lint": "npm-run-all --parallel lint:*",
    "pretest": "npm run lint",
    "test": "npm run mocha",
    "cover": "nyc --reporter=html npm test",
    "//": "删除coverage和.nyc_output文件夹",
    "cover:clean": "rimraf coverage && rimraf .nyc_output",
    "//": "如果不存在coverage_archive\\%npm_package_version%文件夹, 则创建, 并把coverage文件夹下的所有文件复制进去",
    "cover:archive": "if not exist coverage_archive\\%npm_package_version% mkdir coverage_archive\\%npm_package_version% && copy coverage\\* coverage_archive\\%npm_package_version%",
    "//": "以coverage_archive\\%npm_package_version%为根目录, %npm_package_config_port%为端口，创建服务器",
    "cover:serve": "http-server coverage_archive\\%npm_package_version% -p %npm_package_config_port%",
    "cover:open": "open-cli http://localhost:%npm_package_config_port%",
    "postcover": "npm-run-all cover:archive cover:clean --parallel cover:serve cover:open"
  },
  "keywords": [],
  "author": "xusp <xspstudyemail@163.com>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.3.10",
    "eslint": "^8.52.0",
    "http-server": "^14.1.1",
    "jsonlint": "^1.6.3",
    "markdownlint-cli": "^0.37.0",
    "mocha": "^10.2.0",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.1.0",
    "open-cli": "^7.2.0",
    "rimraf": "^5.0.5",
    "stylelint": "^15.11.0",
    "stylelint-config-standard": "^34.0.0"
  }
}